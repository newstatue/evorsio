services:
  postgres:
    image: postgres:17
    container_name: evorsio-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: evorsio
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
