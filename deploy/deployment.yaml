apiVersion: apps/v1
kind: Deployment
metadata:
  name: evorsio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: evorsio
  template:
    metadata:
      labels:
        app: evorsio
    spec:
      containers:
        - name: evorsio
          image: ghcr.io/newstatue/evorsio:sha-c6c39b2cea35869320feeacec4b58904b486dafc
          ports:
            - containerPort: 8080